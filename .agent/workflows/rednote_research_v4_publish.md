---
description: RedNote Research V4.0 - Step 6 内容丰富 & 发布模块
---

# Step 6: 内容丰富 & 发布 (Content Enrichment & Publishing)

**目标**：
1. 生成吸引眼球的封面图
2. 优化内容可读性（表情符号美化）
3. 将研究报告转换为小红书格式
4. 发布到小红书平台

> [!IMPORTANT]
> **小红书平台限制**
> - 标题：≤20 字（超出会被截断）
> - 正文：≤1000 字（超出无法发布）
> - 图片：1-9 张（建议 3-5 张，图文流量最佳）
> - 标签：建议 3-5 个（使用 # 开头）

---

## Step 6A: 封面图智能生成

### 三阶段生成策略

```python
def generate_cover_image(notes_data, analysis_data, topic):
    """
    生成高质量封面图，结合风格分析和内容特征
    """
    # 阶段1: 分析笔记封面图风格
    style_analysis = analyze_cover_styles(
        cover_images=[note['image_urls'][0] for note in notes_data if note['image_urls']],
        aspects=[
            "color_palette",      # 色调分析
            "composition",        # 构图特点
            "text_overlay",       # 文字覆盖风格
            "visual_elements"     # 视觉元素（食物/风景/人物）
        ]
    )
    # 输出示例: {"dominant_colors": ["#FF6B6B", "#FFE66D"], "style": "minimalist", "has_text": true}
    
    # 阶段2: 提取当前内容核心要素
    content_features = extract_content_features(
        title=topic,
        key_findings=analysis_data['key_findings'][:3],
        keywords=analysis_data['top_keywords']
    )
    # 输出示例: {"main_subject": "深圳美食", "highlights": ["烤肉", "海鲜", "甜品"], "mood": "欢快"}
    
    # 阶段3: 生成吸引力优化的 Prompt
    cover_prompt = build_cover_prompt(
        style=style_analysis,
        content=content_features,
        optimization_goals=[
            "eye_catching",       # 视觉冲击力
            "unique_style",       # 与众不同
            "platform_optimized" # 小红书风格适配
        ]
    )
    
    # 生成封面图
    cover_image = generate_image(
        Prompt=cover_prompt,
        ImageName=f"cover_{topic.replace(' ', '_')}"
    )
    
    # 保存到 publish 目录
    save_image(cover_image, f"{PUBLISH_DIR}/cover_image.png")
    
    return f"{PUBLISH_DIR}/cover_image.png"
```

### Prompt 模板

**基础模板**：
```
创建一张小红书封面图，主题：{topic}

风格参考：
- 色调：{dominant_colors}（温暖/清新/高级感）
- 构图：{composition_style}（居中/三分法/对角线）
- 文字：{text_style}（简洁标题/无文字）

核心元素：
- 主体：{main_subject}（食物特写/场景全景）
- 亮点：{highlights}（用视觉符号突出）
- 情绪：{mood}（欢快/温馨/高端）

优化要求：
✨ 视觉冲击力强，3秒内抓住眼球
🎨 色彩鲜明但不俗气，符合小红书审美
📐 画面简洁不杂乱，留白得当
🔥 具有独特性，避免模板化

技术规格：
- 尺寸：1080x1440px（3:4竖版）
- 风格：摄影级真实感 + 轻微滤镜
- 质量：高清无水印
```

**美食类示例**：
```
创建一张小红书封面图，主题：深圳元旦美食推荐

风格参考：
- 色调：暖橙色 + 奶油黄（温暖食欲感）
- 构图：俯视45°角（展示食物全貌）
- 文字：简洁标题"必吃榜"

核心元素：
- 主体：烤肉特写，炭火烟雾缭绕
- 亮点：鲜嫩肉质、金黄色泽
- 情绪：温暖、诱人、欢聚

优化要求：
✨ 食物质感突出，让人看了就流口水
🎨 暖色调为主，搭配木质餐桌纹理
📐 留白20%，突出食物主体
🔥 避免过度修图，保持真实感

技术规格：
- 尺寸：1080x1440px
- 风格：自然光摄影 + 饱和度+10%
- 质量：高清，8K渲染
```

---

## Step 6B: 表情符号智能插入

### 插入规则

```python
def enrich_content_with_emojis(content, analysis_data):
    """
    在正文中智能插入表情符号，提升可读性和互动性
    """
    # 定义表情符号库
    emoji_map = {
        # 类型标记
        "推荐": "💡",
        "必打卡": "📍",
        "避雷": "⚠️",
        "惊喜": "✨",
        "攻略": "📝",
        
        # 内容类别
        "美食": "🍜",
        "旅游": "✈️",
        "购物": "🛍️",
        "拍照": "📷",
        "住宿": "🏨",
        
        # 时间相关
        "营业时间": "⏰",
        "预订": "📅",
        "排队": "⏳",
        "节假日": "🎉",
        
        # 价格相关
        "价格": "💰",
        "优惠": "🎁",
        "性价比": "💯",
        "免费": "🆓",
        
        # 互动引导
        "提示": "💭",
        "问题": "❓",
        "分享": "💬",
        "收藏": "⭐",
        "点赞": "👍"
    }
    
    # 规则1: 段落开头添加主题表情
    enriched_content = add_section_emojis(content, emoji_map)
    
    # 规则2: 关键词前插入相关表情
    enriched_content = highlight_keywords_with_emojis(
        enriched_content,
        keywords=analysis_data['top_keywords'],
        emoji_map=emoji_map
    )
    
    # 规则3: 列表项自动编号美化
    enriched_content = beautify_lists(enriched_content)
    
    # 规则4: 互动引导句增强
    enriched_content = add_engagement_emojis(enriched_content)
    
    return enriched_content
```

### 转换示例

**转换前**（原始报告摘要）：
```
深圳元旦美食推荐

这5家餐厅值得一试：
1. 烤肉店 - 人均150元，炭火现烤
2. 海鲜餐厅 - 需要提前预订，龙虾很新鲜
3. 甜品店 - 网红打卡地，拍照超好看

注意事项：
营业时间是11:00-22:00
周末需要排队1小时
建议避开高峰期

你去过哪几家？评论区分享
```

**转换后**（表情符号美化）：
```
🍜 深圳元旦美食推荐

这5家餐厅值得一试：
📍 1. 烤肉店 - 💰人均150元，炭火现烤
📍 2. 海鲜餐厅 - 📅需要提前预订，龙虾很新鲜
📍 3. 甜品店 - 📷网红打卡地，拍照超好看

💡 注意事项：
⏰ 营业时间是11:00-22:00
⏳ 周末需要排队1小时
⚠️ 建议避开高峰期

💬 你去过哪几家？评论区分享~
```

---

## Step 6C: 报告转小红书格式

### 转换逻辑

```python
def convert_report_to_xiaohongshu(report_md, analysis_data, notes_data):
    """
    从完整报告中提取精华内容，生成小红书发布格式
    集成封面图生成和表情符号美化
    """
    # 1. 生成标题（≤20字）
    title = generate_title(
        topic=analysis_data['topic'],
        key_findings=analysis_data['key_findings'][:2],  # 最多2个亮点
        max_length=20
    )
    # 示例输出："深圳元旦美食｜5家必打卡餐厅"
    
    # 2. 提取正文核心内容（≤900字，预留100字给表情符号）
    raw_content = extract_highlights(
        report_sections=report_md['sections'],
        analysis_insights=analysis_data['insights'],
        max_length=900,
        structure={
            "opening": 30,          # 吸引句
            "findings": 400,        # 核心发现
            "recommendations": 300, # 行动建议
            "closing": 30,          # 互动引导
            "tags": 140             # 预留标签空间
        }
    )
    
    # 2.1 表情符号美化
    content = enrich_content_with_emojis(raw_content, analysis_data)
    
    # 3. 生成封面图（放在首位）
    cover_image = generate_cover_image(
        notes_data=notes_data,
        analysis_data=analysis_data,
        topic=analysis_data['topic']
    )
    
    # 3.1 选择笔记配图（4张）
    supplement_images = select_top_images(
        notes_data=notes_data,
        analysis_relevance=analysis_data['image_relevance_scores'],
        count=4  # 封面图1张 + 配图4张 = 共5张
    )
    
    # 3.2 组合图片列表
    images = [cover_image] + supplement_images
    
    # 4. 生成标签
    tags = generate_tags(
        content=content,
        topic=analysis_data['topic'],
        max_tags=5
    )
    
    return {
        "title": title,
        "content": content,
        "images": images,  # cover_image(本地路径) + 在线 URL 列表
        "tags": tags
    }
```

### 标题生成规则

**格式**：`{主题}｜{亮点}`（使用全角符号｜分隔）

**示例**：
```python
# 输入
topic = "深圳元旦美食推荐"
key_findings = ["5家必试餐厅", "人均150元", "网红打卡地"]

# 输出（≤20字）
"深圳元旦美食｜5家必打卡餐厅"  # 14字
"西安三日游｜人均2000攻略"      # 13字
"情侣约会地｜深圳Top5餐厅"      # 14字
```

### 正文结构模板

```
【开头吸引】(30字)
元旦假期不知道去哪？深圳这5家餐厅绝对不能错过！

【核心内容】(400字)
📍 1. XX烤肉店
💰 人均：150元
⏰ 营业时间：11:00-22:00
✨ 推荐理由：炭火现烤，肉质鲜嫩...

📍 2. YY海鲜餐厅
💰 人均：200元
📅 注意：需要提前预订
✨ 推荐理由：龙虾超新鲜...

【行动建议】(300字)
💡 预订建议：
- 提前1天电话预订
- 工作日去人少

⚠️ 避雷提示：
- 周末排队1小时
- 避开晚上7-8点高峰

【互动引导】(30字)
💬 你去过哪几家？评论区分享你的美食推荐~

【标签区】(140字)
#深圳美食 #元旦推荐 #烤肉 #海鲜 #情侣约会
```

---

## Step 6D: 发布执行

### 发布流程

```python
# Step 6D.1: 内容准备
publish_draft = convert_report_to_xiaohongshu(
    report_md=load_markdown(f"{REPORT_DIR}/final_report.md"),
    analysis_data=load_json(f"{DATA_DIR}/analysis.json"),
    notes_data=load_json(f"{DATA_DIR}/notes_detail.json")
)

# 保存发布草稿供审查
save_json(f"{PUBLISH_DIR}/xiaohongshu_draft.json", publish_draft)

# Step 6D.2: 用户审查（可选）
print_publish_preview(publish_draft)
user_confirmed = input("确认发布? (y/n): ")

IF user_confirmed == "y":
    # Step 6D.3: 发布
    result = mcp_xiaohongshu-mcp_publish_content(
        title=publish_draft['title'],
        content=publish_draft['content'],
        images=publish_draft['images'],  # [本地路径, URL, URL, ...]
        tags=publish_draft['tags']
    )
    
    # Step 6D.4: 记录发布结果
    publish_log = {
        "timestamp": get_current_time(),
        "title": publish_draft['title'],
        "note_id": result.get('note_id'),
        "status": "success" if result else "failed",
        "error": result.get('error'),
        "url": result.get('url')
    }
    save_json(f"{PUBLISH_DIR}/publish_log.json", publish_log, mode="append")
    
    print(f"✅ 发布成功！笔记链接：{result.get('url')}")
ELSE:
    print("❌ 发布已取消，草稿已保存到 {PUBLISH_DIR}/xiaohongshu_draft.json")
```

### 错误处理

| 错误类型 | 处理策略 |
|---------|---------|
| 标题超长 | 自动截断至20字 + 警告 |
| 正文超长 | 自动截断至1000字 + 警告 |
| 图片数量不足 | 使用封面图填充（最少1张）|
| 图片数量过多 | 只取前9张 |
| 登录状态失效 | 提示用户重新登录 |
| 内容违规 | 返回详细错误信息 |
| 发布失败 | 保存草稿，返回错误详情 |

---

## Step 6E: 发布后优化（可选）

### 自动互动

```python
# 发布成功后，自动执行以下操作（可选）
IF result.get('note_id'):
    # 1. 自动回复评论（可选）
    # 监控新评论，自动感谢用户
    
    # 2. 数据统计
    # 24小时后统计点赞、收藏、评论数据
```

---

## 输出格式

### 发布草稿示例

**文件**：`{PUBLISH_DIR}/xiaohongshu_draft.json`

```json
{
  "title": "深圳元旦美食｜5家必打卡餐厅",
  "content": "🍜 元旦假期不知道去哪？深圳这5家餐厅绝对不能错过！\n\n📍 1. XX烤肉店\n💰 人均：150元\n...\n\n#深圳美食 #元旦推荐",
  "images": [
    "D:/work/workspace/1227/prompt_engineering/output/深圳美食_20251228/publish/cover_image.png",
    "http://sns-webpic-qc.xhscdn.com/202512272338/4362b9751745b739ff98370e515c5dc3/...",
    "http://sns-webpic-qc.xhscdn.com/...",
    "http://sns-webpic-qc.xhscdn.com/...",
    "http://sns-webpic-qc.xhscdn.com/..."
  ],
  "tags": ["深圳美食", "元旦推荐", "烤肉", "海鲜", "情侣约会"]
}
```

### 发布日志示例

**文件**：`{PUBLISH_DIR}/publish_log.json`

```json
[
  {
    "timestamp": "2025-12-28T00:15:30+08:00",
    "title": "深圳元旦美食｜5家必打卡餐厅",
    "note_id": "68f89d9a0000000007035440",
    "status": "success",
    "url": "https://www.xiaohongshu.com/explore/68f89d9a0000000007035440"
  }
]
```

---

*V4.0 更新时间：2025-12-28*
